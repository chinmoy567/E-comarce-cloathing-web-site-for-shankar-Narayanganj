/**
 * Frontend constants matching backend (02-customer §2.4).
 * These must stay in sync with backend/src/config/constants.ts.
 */

export const CUSTOMER_ACCESS_COOKIE = 'customer_at';
export const CUSTOMER_REFRESH_COOKIE = 'customer_rt';
